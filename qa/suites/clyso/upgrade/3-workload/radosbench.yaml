tasks:
- cephadm.shell:
    host.a:
      - ceph osd pool create test-pool 32
      - ceph osd pool set test-pool size 2 --yes-i-really-mean-it
      - ceph osd pool application enable test-pool rados
      - rados bench -p test-pool 150 write --no-cleanup
      - rados bench -p test-pool 60 seq
