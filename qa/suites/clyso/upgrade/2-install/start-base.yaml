tasks:
# Install system packages via pexec (avoids CEPH RPM installation)
- pexec:
    all:
    - sudo dnf install s3cmd curl jq -y

- cephadm:

- cephadm.shell:
    env: [base_image_name]
    host.a:
      - ceph status
      - ceph orch ps
      - ceph version
      - echo "Starting with $base_image_name"

overrides:
  ceph:
    conf:
      osd:
        osd shutdown pgref assert: true
    log-only-match:
      - CEPHADM_
    log-ignorelist:
      - CEPHADM_DAEMON_PLACE_FAIL
      - CEPHADM_FAILED_DAEMON
      - CEPHADM_STRAY_DAEMON
      - CEPHADM_AGENT_DOWN
